services:
  ffmpeg-engine:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-8000}:8000"
    volumes:
      - ./media:/app/media:ro
      - ./renders:/app/renders
